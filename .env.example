# Cloudflare API Credentials
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone:DNS:Edit for your domain
CF_API_EMAIL=your-email@example.com
CF_DNS_API_TOKEN=your-cloudflare-api-token-here

# Cloudflare Tunnel Token
# Create a tunnel at: https://one.dash.cloudflare.com/ -> Zero Trust -> Networks -> Tunnels
# Copy the token from the tunnel setup page
TUNNEL_TOKEN=your-tunnel-token-here

# PostgreSQL Database Credentials
# Change these to secure values for production
POSTGRES_USER=n8n
POSTGRES_PASSWORD=change-this-secure-password
POSTGRES_DB=n8n

# Domain Configuration
# Your base domain (should match what's in traefik.yml)
DOMAIN=yourdomain.com

# ================================
# Remote Access Configuration
# ================================

# Twingate Connector Tokens
# Get these from: https://[yournetwork].twingate.com/settings/connectors
# 1. Click "Add Connector" -> "Generate Tokens"
# 2. Copy all three values below
TWINGATE_ACCESS_TOKEN=your-twingate-access-token-here
TWINGATE_REFRESH_TOKEN=your-twingate-refresh-token-here
TWINGATE_NETWORK=yournetwork.twingate.com

# RustDesk Server Configuration (Optional - uses defaults if not set)
# Relay server address - will auto-detect your VM IP if not specified
# Format: IP:PORT (default port is 21117)
RUSTDESK_RELAY_SERVER=192.168.1.11:21117

# Enforce encrypted connections only (1=yes, 0=no)
# Recommended: 1 (encrypted only)
RUSTDESK_ENCRYPTED_ONLY=1
